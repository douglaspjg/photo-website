console.log("Hello World")

const images = document.querySelector('.images-list');

Array.from(images).forEach(function(li) {
    if (!li.child.classList.contains('curent-image') {
        li.child.style.
    }

}

)

console.log(images);